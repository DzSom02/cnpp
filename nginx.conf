server {
    listen 80;
    
    # Define the location for /cnpp
    location /cnpp {
        alias /usr/share/nginx/html;
        index index.html;
    }

    # Optionally, handle other locations (e.g., return 404 for unknown paths)
    location / {
        return 404;
    }
}
